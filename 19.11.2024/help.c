#include <stdio.h>

int main(void) {
    printf("strlength -> returns the length of the string \n");
    printf("strconcat -> combines two strings into one \n");
    printf("strcompare -> compares two strings \n");
}